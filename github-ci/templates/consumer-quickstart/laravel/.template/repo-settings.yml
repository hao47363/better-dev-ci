# Copy to: .template/repo-settings.yml
# Empty commands.* → Laravel stack defaults (composer install, pint --test, artisan test).

project:
  stack: laravel

commands:
  install: ""
  lint: ""
  test: ""
  build: ""

ci:
  run_lint: true
  run_test: true
  run_build: false
