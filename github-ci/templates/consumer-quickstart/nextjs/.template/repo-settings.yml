# Copy to: .template/repo-settings.yml
# Empty commands.* → npm stack defaults (see scripts/run_project_checks.sh).

project:
  stack: nextjs

commands:
  install: ""
  lint: ""
  test: ""
  build: ""

ci:
  run_lint: true
  run_test: true
  run_build: true
